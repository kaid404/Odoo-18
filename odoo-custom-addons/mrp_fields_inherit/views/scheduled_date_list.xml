<odoo>

    <record id="inherit_schedule_date_list_view" model="ir.ui.view">
        <field name="name">inherit_schedule_date_list_view</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="date_finished"/>
            </xpath>
        </field>
    </record>

    <record id="inherit_mrp_view_form" model="ir.ui.view">
        <field name="name">inherit_mrp_view_form</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view"/>
        <field name="arch" type="xml">

            <xpath expr="//label[3]" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//div[@class='o_row'][.//field[@name='lot_producing_id']]" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//div[@class='o_row g-0 d-flex'][.//field[@name='product_qty']]" position="after">
                <label for="lot_producing_id" invisible="state == 'draft' or product_tracking in ('none', False)"/>
                <div class="o_row" invisible="state == 'draft' or product_tracking in ('none', False)">
                    <field name="lot_producing_id" context="{'default_product_id': product_id}"
                           invisible="product_tracking in ('none', False)"/>
                    <button name="action_generate_serial" type="object" class="btn btn-primary fa fa-plus-square-o"
                            aria-label="Creates a new serial/lot number" title="Creates a new serial/lot number"
                            role="img" invisible="product_tracking in ('none', False) or lot_producing_id"/>
                </div>
            </xpath>
        </field>
    </record>

</odoo>